# 任务交接文档：Lyra 武器系统三功能开发

---

## 【任务概述】

| 项目 | 内容 |
|---|---|
| **任务领域** | Unreal Engine 5 C++ 游戏开发（Gameplay Ability System / GAS） |
| **引擎版本** | UE 5.4（与现有 Lyra 工程保持一致，勿升级） |
| **工程路径** | `d:\UE5_My_Project\LyraStarterGame` |
| **核心模块** | `Source/LyraGame/` |
| **核心目标** | 在已编译可运行的 Lyra Starter Game 上增量开发三个功能：① 武器换弹系统 ② 武器后坐力系统 ③ 自定义霰弹枪武器类型 |
| **预期成果** | 三大功能 C++ 实现 + 单元测试 / PIE 测试工具 + 蓝图数据配置清单，PIE 中运行正常 |
| **技术栈** | UE5 C++、GAS、Enhanced Input System、Lyra 模块化装备框架 |

---

## 【已完成部分（请粘贴已有内容）】

> 以下为已完成的全部源码改动。所有文件均通过 VS Code IntelliSense 静态诊断（0 error），但**尚未经过真实编译**——这是本任务的当前断点，详见「之前进度说明」。

### 2.1 换弹系统（任务一）

| 文件 | 操作 | 关键内容 |
|---|---|---|
| `Source/LyraGame/Weapons/LyraRangedWeaponInstance.h` | 修改 | 新增 `MaxAmmo=30`、`ReloadTime=2.0f`（`EditAnywhere`）、`CurrentAmmo=30`（`Transient`）；新增方法 `CanFire()`、`ConsumeAmmo(int32 Amount=1)`、`RefillAmmo()` |
| `Source/LyraGame/Weapons/LyraRangedWeaponInstance.cpp` | 修改 | `OnEquipped()` 初始化 `CurrentAmmo = MaxAmmo`；实现 `ConsumeAmmo`（钳制 ≥0）、`RefillAmmo`（回满） |
| `Source/LyraGame/Weapons/LyraGameplayAbility_Reload.h` | **新增** | 继承 `ULyraGameplayAbility_FromEquipment`；`ReloadMontage`（`EditDefaultsOnly`）；重写 `ActivateAbility` / `EndAbility`；`ReloadTimerHandle` + `bReloadComplete` 状态 |
| `Source/LyraGame/Weapons/LyraGameplayAbility_Reload.cpp` | **新增** | `ActivateAbility` → `CommitAbility` → 播蒙太奇 → `TimerManager.SetTimer(ReloadTime)` → `OnReloadDelayFinished` → `RefillAmmo` → `EndAbility`；`EndAbility` 清理定时器 |
| `Source/LyraGame/Weapons/LyraGameplayAbility_RangedWeapon.cpp` | 修改 | `CanActivateAbility` 先判断 `WeaponInstance->CanFire()`，为 false 则阻止激活；`OnTargetDataReadyCallback` Commit 成功后 `ConsumeAmmo(1)` + `ApplyRecoil()` |

### 2.2 后坐力系统（任务二）

| 文件 | 操作 | 关键内容 |
|---|---|---|
| `Source/LyraGame/Weapons/LyraRangedWeaponInstance.h` | 修改 | 编辑属性 `RecoilPitchMin/Max=0.3/0.8`、`RecoilYawMin/Max=-0.2/0.2`、`RecoilRecoveryRate=6.0`；运行时状态 `PendingRecoilPitch/Yaw`；方法 `ApplyRecoil()`、`UpdateRecoilRecovery(DeltaSeconds)`；补充 5 个 recoil getter 供测试读取 |
| `Source/LyraGame/Weapons/LyraRangedWeaponInstance.cpp` | 修改 | `ApplyRecoil()`：仅本地 `IsLocalController()` 下施加 `AddPitchInput(-PitchDelta)` + `AddYawInput(YawDelta)`，并累计 `PendingRecoilPitch/Yaw`；`Tick` 调用 `UpdateRecoilRecovery`，用 `FMath::FInterpTo` 算衰减量后反向回正 |
| `Source/LyraGame/Weapons/LyraGameplayAbility_RangedWeapon.cpp` | 修改 | 开火成功后调用 `ApplyRecoil()` |

### 2.3 自定义霰弹枪（任务三）

| 文件 | 操作 | 关键内容 |
|---|---|---|
| `Source/LyraGame/Weapons/LyraShotgunWeaponInstance.h` | **新增** | 继承 `ULyraRangedWeaponInstance`；`PelletCount=8`、`SpreadAngle=5.0f`（度）；重写 `OnEquipped` |
| `Source/LyraGame/Weapons/LyraShotgunWeaponInstance.cpp` | **新增** | 构造默认值 `MaxAmmo=6`、`ReloadTime=2.5`、`BulletsPerCartridge=PelletCount`、种子化 `HeatToSpreadCurve` 为常数 `SpreadAngle`；`OnEquipped` 保持 Pellet↔BulletsPerCartridge 同步并补全空曲线 |

**架构设计要点**：霰弹枪刻意**不新建**开火能力，复用 `ULyraGameplayAbility_RangedWeapon::TraceBulletsInCartridge`。该函数天然 `for (BulletIndex < BulletsPerCartridge)` 循环多次发射 + 独立 LineTrace，每条弹丸单独走 `VRandConeNormalDistribution(SpreadAngle)` 散布，伤害逐个命中目标分别应用，与霰弹语义完全匹配。

### 2.4 测试与验证工具

| 文件 | 操作 | 关键内容 |
|---|---|---|
| `Source/LyraGame/Tests/LyraAmmoReloadTest.cpp` | **新增** | 两个 UE Automation Test：`FLyraRangedWeaponAmmoTest`（弹药状态机 7 阶段）、`FLyraShotgunDefaultsTest`（霰弹枪构造默认值） |
| `Source/LyraGame/LyraGame.Build.cs` | 修改 | 新增 `"AutomationTest"` 模块依赖 |
| `Source/LyraGame/Player/LyraCheatManager.h` | 修改 | 新增 `TestRecoil` exec 命令、`FRecoilTestState` 状态结构、3 个私有回调函数 |
| `Source/LyraGame/Player/LyraCheatManager.cpp` | 修改 | 实现 `TestRecoil`（连发开火 + 采样 ControlRotation + 恢复阶段 + PASS/FAIL 报告） |

### 2.5 Bug 修复

- **Issue1 后坐力方向反转**：UE5 中 `AddPitchInput` 正值向下、负值向上，原 `AddPitchInput(PitchDelta)` 导致枪口向下。已改为 `AddPitchInput(-PitchDelta)`，并同步修正 `UpdateRecoilRecovery` 的回正方向为 `AddPitchInput(+PitchRecover)`。

---

## 【当前进度说明】

### 已完成的要点

- ✅ **要点 1（换弹系统）**：弹药属性 + `CanFire/ConsumeAmmo/RefillAmmo` + `ULyraGameplayAbility_Reload` 蒙太奇 + 定时器补弹流程 + 开火能力拦截，全部 C++ 代码完成。
- ✅ **要点 2（后坐力系统）**：后坐力属性 + `ApplyRecoil` 施加视角偏移 + `Tick` 中 `FInterpTo` 平滑回正 + Issue1 方向修复，全部 C++ 代码完成。
- ✅ **要点 3（霰弹枪）**：`ULyraShotgunWeaponInstance` 继承实现 + 复用开火技能的数据表达方案，全部 C++ 代码完成。
- ✅ **要点 4（测试工具）**：弹药 Mock 单元测试 + `TestRecoil` PIE 测试命令 + recoil getter 补充，全部代码完成。

### 尚未完成的部分（断点位置）

**断点位置：C++ 代码 100% 完成，但【未编译验证】，且【蓝图侧配置 0% 开始】。**

> 精确断点：`Source/LyraGame/` 目录下所有改动源码均已就绪（文件路径见「已完成部分」各表），下一步停在「VS2022 编译验证」这一**阶段节点**，之后进入「UE 编辑器蓝图数据配置」阶段。

---

## 【待完成的具体要求】

### 要求 1：VS2022 编译验证（先决条件）

- **目标**：所有新增/修改 C++ 代码在真实编译下 0 error。
- **技术规范**：解决方案配置 `Development Editor`，平台 `Win64`，目标项目 `LyraEditor`。
- **质量标准**：`生成成功`，无 C2679/C2440/C2504 等编译/链接错误。
- **交付物**：编译通过后的 `LyraEditor` 可执行模块。

### 要求 2：运行自动化测试（证明弹药逻辑正确）

- **目标**：`Lyra.Weapons.Ranged.Ammo` 与 `Lyra.Weapons.Shotgun.Defaults` 两个用例全部 ✅ Pass。
- **可量化完成指标**：Result 显示 2/2 Pass、0 Fail。

### 要求 3：蓝图数据配置（端到端联调，共 7 项）

1. **GameplayTag**：Project Settings → Gameplay Tags 新增 `InputTag.Weapon.Reload`（已存在则跳过）。
2. **换弹能力蓝图 GA_Weapon_Reload**：新建蓝图，父类 `LyraGameplayAbility_Reload`；Activation Policy=`OnInputTriggered`、Activation Group=`Exclusive_Replaceable`、ReloadMontage 指向换弹 AnimMontage（无现成资产可留空）。
3. **InputData 绑定**：`InputData_Hero` 的 `AbilityInputActions` 数组加一条——InputAction=`IA_Weapon_Reload`（已存在）、InputTag=`InputTag.Weapon.Reload`。
4. **IMC 键位映射**：确认 `IMC_Default_KBM` 中 `IA_Weapon_Reload` → `Keyboard R` 映射存在，无则补。
5. **能力授予**：武器 EquipmentDefinition 的 `AbilitySetsToGrant` 引用的 AbilitySet 中 GrantedAbilities 添加两条——`GA_Weapon_Fire`（已存在）→ `InputTag.Weapon.Fire`，`GA_Weapon_Reload`（新）→ `InputTag.Weapon.Reload`。
6. **武器数值配置**：
   - 步枪（`LyraRangedWeaponInstance` 子类）：`MaxAmmo=30`、`ReloadTime=2.0`、`RecoilPitchMin/Max=0.3/0.8`、`RecoilYawMin/Max=-0.2/0.2`、`RecoilRecoveryRate=6.0`。
   - 霰弹枪（`LyraShotgunWeaponInstance` 子类）：`PelletCount=8`、`SpreadAngle=5.0`、`MaxAmmo=6`、`ReloadTime=2.5`、后坐力参数略大于步枪。
7. **地图放置**：拖 `ALyraWeaponSpawner` Actor，`PickUpDefinition` 引用霰弹枪 InventoryItemDefinition，或直接放置霰弹枪武器 Actor。

### 要求 4：PIE 端到端验证

| 验证项 | 操作 | 预期结果（可量化指标） |
|---|---|---|
| 开火消耗 | 拾步枪按住左键连射 | `CurrentAmmo` 每次 -1；30 发后无法再开火 |
| 换弹补满 | 打空/中途按 R | 2.0s 后 `CurrentAmmo` 回 30 |
| 后坐力方向 | 连发步枪 | 枪口**向上**抬，停火约 0.3s 平滑回正 |
| 后坐力测试 | 控制台 `TestRecoil 10` | REPORT 行 RESULT=PASS，`PitchKicked=YES` 且 `PitchRecovered=YES` |
| 霰弹多弹丸 | 近距离射击目标 | 命中 8 个弹道点，伤害叠加 |
| 霰弹散布 | 远距射墙 | 8 弹孔分布在约 5° 锥形内 |
| 霰弹弹药 | 连续射击 | 6 发后停火，R 补满 6 发 |

---

## 【风格与衔接要求】

- **语气风格**：正式技术文档风格，客观陈述，避免口语化。
- **专业术语体系**：统一使用 Lyra/GAS 官方术语（如 `AbilitySetsToGrant`、`InputTag`、`EquipmentDefinition`、`GameplayAbility` 等），不自行造词。
- **文档格式规范**：Markdown；标题层级遵循 `#`（一级）→ `##`（二级，对应文档各【】节）→ `###`（三级，对应功能分组）；文件清单用表格，验证项用表格，要点用列表。
- **命名风格**：
  - 变量/属性：Lyra 风格 `PascalCase`（如 `MaxAmmo`、`CurrentAmmo`、`PendingRecoilPitch`）。
  - 文件命名：`Lyra + 功能` 前缀（如 `LyraRangedWeaponInstance.h`、`LyraGameplayAbility_Reload.cpp`）。
  - 蓝图资产命名：`GA_`（Gameplay Ability）、`IA_`（Input Action）、`IMC_`（Input Mapping Context）、`B_`（Blueprint）、`SM_`（Static Mesh）。
  - 代码注释：**中文**（本任务后续改动约定）。

---

## 【辅助信息需求】

1. **任务的具体领域或类型**：Unreal Engine 5 C++ 游戏开发，聚焦 Gameplay Ability System（GAS）与 Lyra 模块化装备框架。
2. **任务的核心目标与预期成果**：在 Lyra Starter Game 上增量实现换弹系统、后坐力系统、自定义霰弹枪三个功能，预期 PIE 中运行正常。
3. **已完成部分的主要内容概述**：见「已完成部分」——三大功能 C++ 源码 + 单元测试/PIE 测试工具 + 一处方向 Bug 修复，均已静态诊断 0 error。
4. **当前任务的具体断点位置描述**：C++ 代码全部完成但未编译；蓝图侧配置未开始。下一步从「VS2022 编译验证」阶段节点继续。
5. **待完成部分的详细要求清单**：见「待完成的具体要求」——编译验证、自动化测试、蓝图配置（7 项）、PIE 验证（7 项指标）。

---

## 关键架构说明（接收方必读）

1. **能力授予路径**：Lyra 通过 `InventoryItemDefinition → Fragment_Equippable → EquipmentDefinition → AbilitySetsToGrant → ULyraAbilitySet → GrantedAbilities` 授予能力，**不是**直接在 WeaponInstance 上配 `AbilitiesToGrant`。
2. **输入绑定机制**：`ULyraInputConfig::AbilityInputActions` 将 InputAction 与 InputTag 关联 → `ULyraHeroComponent::BindAbilityActions` 绑定到 ASC → ASC `AbilityInputTagPressed/Released` 触发匹配 InputTag 的能力。
3. **弹药复制（需决策项）**：`CurrentAmmo` 当前为普通 `UPROPERTY(Transient)` 字段，**不自动跨网络复制**。单人 PIE 无影响；若需多人联机弹夹同步，需改为 `ReplicatedUsing` + 重写 `GetLifetimeReplicatedProps`。
4. **后坐力回正算法**：用 `AddPitchInput/AddYawInput` 反向施加补偿，而非直接改 `ControlRotation`，好处是兼容玩家视角 PitchClamp/ViewPitchMin/ViewPitchMax 约束。

## 风险与注意事项

- **编译未验证**：本次所有代码仅通过 IntelliSense 静态诊断，真实编译可能出现 IntelliSense 未覆盖的链接/模板实例化错误。
- **文件编码**：修改过的 `.h/.cpp` 保持 UTF-8 with BOM，若编译报"垃圾字符/非法 token"，先检查 BOM 是否被破坏。
- **蓝图资产已有同类**：工程内已存在 `IA_Weapon_Reload`、`GA_Weapon_AutoReload`、`SM_Shotgun`、`GA_Weapon_Fire` 等资产，配置时若与现有自动换弹逻辑冲突，需新建自己的资产版本，勿直接破坏现有蓝图。
- **霰弹枪特殊伤害**：当前复用步枪框架，未做"近距离多弹丸伤害衰减/叠加上限"，如需后续扩展可基于 `OnRangedWeaponTargetDataReady` 蓝图中的 CartridgeID 分组字段实现。